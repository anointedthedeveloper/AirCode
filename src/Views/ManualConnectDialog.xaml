<Window x:Class="AirCode.Views.ManualConnectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manual Connect" Height="220" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BgBrush}">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Connect Manually" FontSize="17" FontWeight="Bold"
                       Foreground="{StaticResource TextPrimaryBrush}"/>
            <TextBlock Text="Enter the AirCode host IP address." FontSize="12"
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
        </StackPanel>
        <StackPanel Grid.Row="1">
            <TextBlock Text="HOST IP ADDRESS" Style="{StaticResource Label}"/>
            <TextBox x:Name="IpBox" Style="{StaticResource InputBox}" FontFamily="Consolas"
                     Text="192.168.137.1"/>
        </StackPanel>
        <Grid Grid.Row="2"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource GhostButton}" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Connect" Style="{StaticResource PrimaryButton}" Padding="20,8" Click="Connect_Click"/>
        </StackPanel>
    </Grid>
</Window>
