<UserControl x:Class="AirCode.Views.TransfersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgBrush}">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Transfers" Style="{StaticResource PageTitle}"/>
            <TextBlock Text="All file transfers on this session." FontSize="13"
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" Style="{StaticResource MinimalScroll}">
            <ItemsControl x:Name="TransferList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Card}" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="32"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Direction indicator -->
                                <TextBlock Text="{Binding Direction, Converter={StaticResource TransferDir}}"
                                           FontSize="18" FontWeight="Bold"
                                           Foreground="{StaticResource PrimaryBrush}"
                                           VerticalAlignment="Center"/>

                                <!-- File info + progress -->
                                <StackPanel Grid.Column="1" Margin="4,0,16,0">
                                    <TextBlock Text="{Binding FileName}" FontSize="13" FontWeight="SemiBold"
                                               Foreground="{StaticResource TextPrimaryBrush}"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding PeerName}" FontSize="11"
                                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,4"/>
                                    <ProgressBar Value="{Binding Progress}" Maximum="100" Height="3"
                                                 Foreground="{Binding Status, Converter={StaticResource TransferStatusToColor}}"
                                                 Background="{StaticResource BorderBrush}"
                                                 Visibility="{Binding IsActive, Converter={StaticResource BoolToVis}}"/>
                                    <TextBlock Text="{Binding ProgressText}" FontSize="10"
                                               Foreground="{StaticResource TextMutedBrush}" Margin="0,2,0,0"
                                               Visibility="{Binding IsActive, Converter={StaticResource BoolToVis}}"/>
                                </StackPanel>

                                <!-- Size -->
                                <TextBlock Grid.Column="2" Text="{Binding FileSizeText}" FontSize="12"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Center"/>

                                <!-- Status badge -->
                                <Border Grid.Column="3" Padding="8,4" CornerRadius="4"
                                        Background="{Binding Status, Converter={StaticResource TransferStatusToColor}}"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="SemiBold"
                                               Foreground="White"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
